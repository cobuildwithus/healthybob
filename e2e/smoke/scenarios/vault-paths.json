{
  "id": "vault-paths",
  "command": "vault-cli vault paths --vault <path> [--format json|md] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md",
    "fixtures/minimal-vault/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/show",
  "purpose": "Covers explicit vault path inspection so operators can discover the canonical directories exposed by the vault layout."
}
