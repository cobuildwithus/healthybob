{
  "id": "list",
  "command": "vault-cli list --vault <path> [--kind <kind>] [--experiment <slug>] [--date-from <date>] [--date-to <date>] [--cursor <cursor>] [--limit <n>] [--format json|md] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md",
    "fixtures/minimal-vault/journal/2026/2026-03-10.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/list",
  "purpose": "Covers read-model listing semantics, including stable filters and ids that may need conversion to a queryable lookup before show."
}
