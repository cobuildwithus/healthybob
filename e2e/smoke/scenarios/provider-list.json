{
  "id": "provider-list",
  "command": "vault-cli provider list --vault <path> [--status active|inactive] [--limit <n>] [--format json|md] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/bank/providers/README.md",
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/list",
  "purpose": "Documents provider registry listing with optional status filtering."
}
