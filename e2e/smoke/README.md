# Smoke Scenarios

Each scenario manifest maps one documented baseline command to:

- a vault fixture
- any prerequisite input files
- a reserved golden-output directory

`verify-fixtures.mjs` is the executable smoke gate for this scaffold.
