# Sample Import Placeholders

These files are operator-facing input placeholders for import-oriented smoke scenarios.

## Notes

- Markdown and text files here are safe, deterministic stand-ins for raw documents.
- Meal inputs use placeholder text files until binary fixture handling is introduced.
- CSV samples keep column names explicit so import scenarios can bind `--ts-column` and `--value-column` without assuming canonical output fields.
