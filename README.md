# Healthy Bob

Bootstrap repository scaffold for Healthy Bob.

## Current State

- Repository-local agent harness is installed.
- Durable docs live under `agent-docs/`.
- Thin workflow wrappers live under `scripts/`.
- Product/runtime code has not been added yet.

## Next Steps

1. Define the first real subsystem in `ARCHITECTURE.md`.
2. Add repo-specific build, typecheck, and test tooling.
3. Update `agent-docs/operations/verification-and-runtime.md` when those commands exist.
